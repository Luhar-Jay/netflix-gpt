import React from "react";

const VideoBackground = ({ imdb_link }) => {
  //   const getMoviesVideos = async () => {
  //     const data = await fetch(
  //       "https://imdb-top-100-movies.p.rapidapi.com/",
  //       API_OPTIONS
  //     );
  //     const json = await data.json();
  //     console.log(json);

  //     const filterData = json.data.filter(
  //       (video) => video.imdb_link === "https://www.imdb.com/title/tt0111161"
  //     );
  //     console.log(trailer);

  //     const trailer = filterData.length ? filterData[0] : json.data[0];
  //   };

  return <div>VideoBackground</div>;
};

export default VideoBackground;
